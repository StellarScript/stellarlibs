{
   "$schema": "https://json-schema.org/schema",
   "$id": "App",
   "title": "",
   "type": "object",
   "properties": {
      "name": {
         "type": "string",
         "description": "",
         "$default": {
            "$source": "argv",
            "index": 0
         },
         "x-prompt": "What name would you like to use?"
      },

      "project": {
         "type": "string",
         "description": "name the project root directory",
         "x-prompt": "What project would you like name root project?"
      }
   },
   "required": ["name", "project"]
}
