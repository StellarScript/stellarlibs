{
   "$schema": "https://json-schema.org/schema",
   "$id": "App",
   "title": "",
   "type": "object",
   "properties": {
      "name": {
         "type": "string",
         "description": "",
         "$default": {
            "$source": "argv",
            "index": 0
         },
         "x-prompt": "What name would you like to use?"
      },
      "project": {
         "type": "string",
         "description": "name the root project",
         "x-prompt": "What project would you like to use for project name?"
      }
   },
   "required": ["name", "project"]
}
