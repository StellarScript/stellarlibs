const { NxWebpackPlugin } = require('@nx/webpack');
const { join } = require('path');

module.exports = {
   output: {
      path: join(__dirname, '../../../dist/packages/testapp/myapp'),
   },
   plugins: [
      new NxWebpackPlugin({
         target: 'node',
         compiler: 'swc',
         main: './src/lambda.ts',
         tsConfig: './tsconfig.app.json',
         optimization: false,
         outputHashing: 'none',
      }),
   ],
};
